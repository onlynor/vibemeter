version: "3.9"

services:
  web:
    build: .
    container_name: sentiment-insight
    ports:
      - "8092:8092"
    volumes:
      - ./src/data:/app/src/data
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
